<?php
	
	const TABLA_CLASS = "panel panel-warning";  // Clase css que ponemos para que la tabla que muestra los resultados, tenga un color determinado
	const BTN_LOGIN_CLASS = "btn-warning";      // Clase css que ponemos para que el boton de logearse, tenga un color determinado
	const BTN_ACTUALIZAR_CLASS = "btn-warning"; // Clase css que ponemos para que el boton de actualizar, tenga un color determinado
	const SEARCH_CLASS = "btn btn-warning";     // Clase css que ponemos para que la lupa (buscador), tenga un color determinado
	const TITULO = "Agencia B";                 // Titulo que aparece en este Sitio Web en particular
	const NOM_AGENCIA = "'Agencia_B'";          // Nombre que aparece en el campo nombre de la tabla agencias de esta agencia en particular (para cliente javascript)
	const NOMBRE_AGENCIA = "Agencia_B";         // Nombre que aparece en el campo nombre de la tabla agencias de esta agencia en particular (para cliente php)
	const NOMBRE_FEDERACION = "Federacion";     // Nombre que aparece en el campo nombre de la tabla agencias de la agencia federacion (que las engloba todas) 
	const AGENCIA = 2; 						    // id agencia que aparece en el campo id_agencia de la tabla agencias de esta agencia en particular
	const FEDERACION = 4; 					    // id agencia que aparece en el campo id_agencia de la tabla agencias de la agencia Federacion 
	const ACCESO_FEDERACION = 1;                // Valor que indica que el usuario se ha logeado en modo federacion(el usuario no esta asociado a una agencia en concreto)
	const ACCESO_AGENCIA = 0;                   // Valor que indica que el usuario se ha logeado en modo agencia(el usuario esta asociado a una agencia en concreto)

?>